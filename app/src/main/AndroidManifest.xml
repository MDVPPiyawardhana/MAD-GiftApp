<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    package="com.example.payment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_cash_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Payment">
        <activity
            android:name=".choosePaymentMeth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".changePaymentDetails"
            android:label="@string/upDetails"/>
        <activity android:name=".Payment1"
            android:label="@string/insCardDet"/>
        <activity android:name=".MainActivity"
            android:label="@string/addCard">
        </activity>
    </application>

</manifest>